《Java Web 应用程序开发》大作业A班级： 学号（后四位）：________ 姓名：__________ 成绩：要求：（1） JSP+Maven+MySQL 完成，用学过的JSP知识完成要求的功能，不要使用未在课堂上讲授的知识完成。（2）不要使用超过上课所用的样式或布局，以完成功能为主。（3）提交的 Servlet 代码不包含 import 导入类部分；JDBC操作必须调用通用方法来完成。（4）不要采用 fetch、axios 等异步方式完成，应采取form提交、Servlet 处理、显示结果页面形式。（5）保持代码完整性，如某一段代码使用了额外的、自定义方法或类，应在该处加上这些方法或类。（6）上传包含数据库文件的工程压缩文件。（7）录制视频，逐一介绍系统的功能。
有如下旅游景点管理系统：用户登录后，有不同的城市，可以对城市进行管理。每个城市有自己有特色的旅游景点，可以管理旅游景点，也可以为景点上传图片。用户、城市、景点信息都存放在数据库中。
1 显示所有城市：index.jsp 中有该 Servlet 链接。Servlet 得到所有城市，在页面用表格显示所有城市的信息，城市的信息至少要有编号（自增）、名字、所属省，每个城市后有对应的操作超链接：编辑、显示该城市的所有景点。页面有新建超链接，用于新建城市。该页面可以显示新建、编辑等操作的结果。（1.1 得到所有城市 Servlet，1.2 显示所有城市页面）2 新建城市：显示新建城市的输入页面，用户提交后，Servlet 中接收数据并保存在数据库中，准备操作结果信息，转到显示所有城市。（2.1 城市输入页面，2.2 保存城市Servlet）3 编辑城市：Servlet 得到指定 id 的城市，在编辑页面显示原有城市的信息进行编辑。提交后在 Servlet 中更新，准备操作结果信息，转到显示所有城市。（3.1 得到城市Servlet，3.2城市编辑页面，3.3 更新城市 Servlet）
4 显示城市下的所有景点：得到指定城市下的所有景点，用表格显示景点的信息和操作，景点信息至少有 id、名字、门票价格、简介、图片，每个景点只有一张图片。该页面不用显示图片。每个景点对应的操作有上传图片超链接。该页面有增加景点超链接。（4.1得到城市下所有景点 Servlet，4.2 显示所有景点页面）
5 新建景点：显示新建景点页面，输入名字、门票价格、简介，用户提交后，在Servlet 中保存到数据库，转到显示该城市所有景点。（5.1 景点输入页面，5.2 保存景点Servlet）6 上传景点图片：上传页面，在 Servlet 中保存选定的上传图片，不考虑文件重名问题，和景点的关系保存在数据库中。完成后转到显示该景点详细信息页面，显示id、名字、门票价格、简介、图片，显示详细信息页面有显示该城市所有景点的链接。（6.1 上传页面，6.2保存上传图片 Servlet，6.3 得到该景点详细信息 Servlet，6.4 显示该景点详细信息页面）7 数据库通用操作。（7.1 更新操作，7.2 查询操作）
8 HSQLDB 数据库文件 test.script 完整内容（完成代码后每个表应该已有几条数据，停掉服务器和 HSQLDB 数据库服务器后的文件）（8.1 test.script）；若使用mysql，给出表格创建sql 语句，以及每个表中通过 insert 的 SQL 语句插入的记录。